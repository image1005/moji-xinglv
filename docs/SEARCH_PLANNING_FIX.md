# 2026-09-21 联网、图片与落笔编排修复

起点：`E:\hbws\moji-xinglv-new\moji-xinglv-main`，`codex/travel-delivery`，`e9a982f`，工作区干净。重新阅读 AGENTS、PRD、package.json、Bun 锁文件与实际代码。保留 Nuxt/Bun/现有库和统一规划写入事务；无新依赖、无数据库迁移、未写入真实业务数据库或修改 .env。真实库仅用于只读核对工具错误与配置。

## 原因、证据与修复

| 用户现象 | 运行/代码证据 | 修复 |
| --- | --- | --- |
| 所有城市的联网开关灰色 | 原能力函数仅检查 TAVILY_API_KEY；本机无此项，但已有有效的官方 DeepSeek 密钥 | auto 优先 Tavily，否则启用已验证的 DeepSeek Anthropic 官方搜索；不误用忽略 web_search 的 Responses 接口。服务端重算实际提供方并保存到配置快照/幂等身份；UI显示实际提供方 |
| 景点与美食图片缺失 | 图片与搜索开关原本独立。精确标题“苏州拙政园”无条目，“拙政园”可匹配且简介包含苏州市；图鉴在当前规划晚到时未重新加载；HTTP失败后已尝试集合阻断继续；显式重试仍命中1小时负缓存 | 有界城市前缀规范化、仍严格验证城市；监听当前规划就绪；手动重试重新处理失败项/负缓存，自动批次不循环重试；图片身份缓存更新为v2 |
| 落笔编排报错 | 真实模型在杭州两天及北京七天请求中均出现非法 category，随后修正可提交。历史库另有空参数校验失败却将运行记为 completed；未取得该历史请求的原始上游 finish_reason，无法断言其是否被截断 | 工具说明补全合法类别/类型和两天一批的生成规则；保留严格校验，错误可由 Mastra 后续步骤纠正；最终写入失败、输出截断或工具步数耗尽都记为 failed 并提示保留已提交内容。修正成功后可正常完成；清理SDK序列化错误外壳 |

图片服务只展示实际取得、匹配且有署名/许可的资源，不保证所有小众地点或菜名有百科图片；未匹配仍明确显示缺失。百度密钥仍未配置，真实地图/定位没有因此获得验证。

## 验证与复现

- 真实 DeepSeek 官方 Anthropic 搜索：HTTP200，实际 web_search_tool_result。只提取工具返回的来源；无明文摘要时显示缺失提示，不把模型正文链接或 encrypted_content 当作检索证据。
- 修复后 `bun run verify:planning --real --search`：真实 Mastra→DeepSeek→search_web→apply_plan_edits→SQLite 临时库，light 配置；最终复验返回10条来源，1天/4景点/1食记，version=2，工具错误0，finishReason=stop。真实服务调用需 `--real`，使用项目 .env，可产生少量费用。
- 修复前辅助复现：杭州2天/4景点/2食记、北京7天/28景点/7食记分别出现1次类别错误，模型纠正后成功；均只新增一个版本。这不等同于所有城市都已验证。
- 修复后 `bun run verify:media --real`：苏州拙政园与东坡肉图片均实际下载、完整解码并保留来源/署名；使用临时缓存数据库。
- 新增回归覆盖供应商选择与密钥边界、真实原生搜索协议解析/来源真实性/取消、城市名称消歧与负缓存重试、失败资源继续、失败/修正/截断终态与错误展示。发布检查结果见 [VERIFICATION](VERIFICATION.md)。

## 使用与部署

更新后重启正在运行的应用并刷新页面；官方 DeepSeek 部署使用现有 AI_API_KEY/AI_BASE_URL 即可，AI_SEARCH_PROVIDER 默认 auto。第三方兼容地址需配置独立 Tavily；也可明确选择 deepseek/tavily/off。已失败的资源可在图鉴点击重试，旧失败会话可继续生成，已保存版本不被删除。无额外数据迁移步骤；本次未推送或部署。
