/** 工具名清单：tools 注册与聊天流校验共用，避免新增工具时漏改。 */
export const MUTATION_TOOLS = new Set(['apply_plan_edits', 'patch_plan_json'])
export const ALL_TOOLS = new Set(['get_plan', 'apply_plan_edits', 'patch_plan_json', 'get_panorama', 'search_poi'])
